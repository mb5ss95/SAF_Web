html 마크업 언어
css 스타일시트 언어

const 변수는 변경 불가
const 배열, 객체는 변경 가능(재할당 안됨)

전역지역 차이 말하기

타입변환할때 대문자로 써야댐 ex. String(5)

const, let은 호이스팅이 되는걸 방지함

이렇게 쓰는걸 호이스팅
console.log(a);
const a = 19;

ECMAScript
명서세
현재 ES11이 최신
ES6 이후를 모던 javascript라고 표현

객체(object)는
키(key)와
값(value)로 이루어진
프러퍼티(property. 속성)의 모음이다.

클래스 이름 쓸때 test-test 방식으로 카멜

2시간 시험
다음주 7월 25일 월요일 9시~11시 시험
내일 테스트 있음 월말 평가 관련해서~!